#!/usr/bin/python3

def quotient( x, y ):
    return x//y

numerator = int(28)
denominator = int(7)
result = quotient (numerator,denominator)

print(str(numerator) + " divide by "+ str(denominator) + " equals " + str(result))
